# FRACTRAN Compiler

Компилятор эзотерического языка программирования **FRACTRAN**.
В отличие от обычных интерпретаторов, данный проект нацелен на компиляцию математических правил FRACTRAN в исполняемый JavaScript код.

---

## 📖 Коротко о FRACTRAN

**FRACTRAN** — это тюринг-полный язык, созданный Джоном Конвеем. Программа представляет собой упорядоченный список положительных дробей.

**Алгоритм:**
1. Программа получает на вход начальное целое число $n$ и список дробей $f_1, f_2, ..., f_k$.
2. На каждой итерации интерпретатор ищет **первую** дробь $f_i$, при умножении на которую число $n$ остается целым ($n \cdot f_i \in \mathbb{Z}$).
3. Если такая дробь найдена, $n$ обновляется ($n = n \cdot f_i$), и поиск начинается заново с первой дроби списка.
4. Если ни одна дробь не подходит, выполнение программы завершается.

